Drop WordPress plugin .zip files in this folder. They are uploaded and activated
automatically on every WordPress setup run. Files must be valid plugin zip archives
(main plugin file in either the zip root or a folder matching the plugin slug).